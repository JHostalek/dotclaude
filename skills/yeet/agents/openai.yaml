interface:
  display_name: "Yeet"
  short_description: "Implement, merge, and optionally audit a change"
  default_prompt: "Use $yeet to implement, open, and merge this work from the current checkout. Add `with audit` only when a full audit is needed."
policy:
  allow_implicit_invocation: false
